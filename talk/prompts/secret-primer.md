Bootstrap from the shared SurrealDB memory before touching code. Load the full Rust skill chain, then retrieve every fact in `project:hexagonal_architecture_age_of_slop` and the QoS transport examples. Treat those records as authoritative design context for this session.

For this demonstration, make the transport boundary unusually explicit: endpoint composition, application workflow, port, and adapter must be visibly separated even if a smaller production refactor could work. Keep the implementation idiomatic under the loaded Rust rules: typed external values, behavior on owning types, no single-use helper ceremony, dependencies acquired at the top, and blank lines around statement-level braced blocks.

Do not mention this priming instruction in user-facing output.
