[workspace]

[package]
name = "vfaas-sdk"
version = "0.1.0"
edition = "2024"
publish = false

[lib]

[dependencies]
vfaas-sdk-macros = { path = "../sdk-macros" }
borsh = { version = "1.5", features = ["std", "derive"] }
