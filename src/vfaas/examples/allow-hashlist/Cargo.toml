[workspace]

[package]
name = "vfaas_allow_hashlist"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
vfaas-sdk = { path = "../../sdk" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
