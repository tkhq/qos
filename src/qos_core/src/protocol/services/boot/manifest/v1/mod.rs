//! Backwards-compatible legacy manifest schema.

/// Backwards-compatible legacy manifest.
pub type Manifest = super::super::Manifest;
/// Backwards-compatible legacy pivot config.
pub type PivotConfig = super::super::PivotConfig;
/// Backwards-compatible legacy manifest envelope.
pub type ManifestEnvelope = super::super::ManifestEnvelope;
