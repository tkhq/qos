[workspace]

[package]
name = "allow_hashlist_policy"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
qos_wasm_sdk = { path = "../../../wasm_sdk" }
qos_wasm_sdk_macros = { path = "../../../wasm_sdk_macros" }

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
opt-level = "s"
lto = true
