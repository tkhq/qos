apiVersion: apps/v1
kind: Deployment
metadata:
  name: qos-enclave
  labels:
    qos.turnkey.io: app
spec:
  template:
    spec:
      containers:
        - name: qos-enclave
          image: ghcr.io/tkhq/qos_enclave
          command:
            - /qos_enclave
